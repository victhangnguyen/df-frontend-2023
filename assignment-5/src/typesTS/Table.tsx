export interface TableBookItem {
  idx: number
  name: string
  author: string
  topic: string
  action: React.JSX.Element
}

export interface TableTopicItem {
  idx: number
  name: string
  action: React.JSX.Element
}
