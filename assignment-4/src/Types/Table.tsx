export interface BookTableItem {
  idx: number
  name: string
  author: string
  topic: string
  action: React.JSX.Element
}

export interface TopicTableItem {
  idx: number
  name: string
  action: React.JSX.Element
}
